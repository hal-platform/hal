parameters:

    routes.api.crud:

        api.build.start:
            method: 'POST'
            route: '/api/applications/:application/build'
            stack: ['middleware.api.token', 'middleware.api.require_entity', 'middleware.requestBody', 'api.build.start']
            conditions:
                application: %routes.regex.digit%
