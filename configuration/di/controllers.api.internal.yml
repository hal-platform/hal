services:

    ####################################################################################################################
    # INTERNAL API CONTROLLERS
    #
    # Used by application frontend only, not for public use.
    ####################################################################################################################

    api_internal.deployment.status:
        class: 'Hal\UI\Controllers\ApiInternal\DeploymentStatusController'
        arguments:
            - '@doctrine.em'
            - '@pools'
            - '@permissions'
            - '@response.formatter'
            - '@hal.application'
            - '@hal.environment'
            - '@currentUser'
