{# FLASH MESSAGES #}
{% import "_partial/macros.twig" as macros %}

{% block flash %}
    {% if session_flash(true) %}
        {% for message in session_flash(false) %}
            {{ macros.alert(message.type, message, message.details) }}
        {% endfor %}
    {% endif %}
{% endblock %}

{# GLOBAL MESSAGE #}
{% block messages %}
    {% if globalMessage() %}
        {{ globalMessage()|raw }}
    {% endif %}
{% endblock %}
