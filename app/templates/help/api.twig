{% extends 'base.html.twig' %}

{% set page_title = 'API Documentation' %}

{% block content %}
    <h2>API Documentation</h2>

    <h3>Conventions</h3>

    <p>Formats, conventions, pagination, etc.</p>

    <h4>Navigation</h4>

    <p>Links and stuff.</p>

    <h3>Public Endpoints</h3>

    <p>Public endpoints offer you insight into the build and push process without being able to add, remove, or change anything.</p>

    <h4>Index</h4>

    <ul>
        <li><a href="{{ urlFor('api.index') }}">{{ urlFor('api.index') }}</a></li>
    </ul>



    <h4>Environments</h4>

    <ul>
        <li><a href="{{ urlFor('api.environments') }}">{{ urlFor('api.environments') }}</a></li>
        <li><a href="{{ urlFor('api.environment', {id: 'id'}) }}">{{ urlFor('api.environment', {id: ':id'}) }}</a></li>
    </ul>

    <h4>Servers</h4>

    <ul>
        <li><a href="{{ urlFor('api.servers') }}">{{ urlFor('api.servers') }}</a></li>
        <li><a href="{{ urlFor('api.server', {id: ':id'}) }}">{{ urlFor('api.server', {id: ':id'}) }}</a></li>
    </ul>

    <h4>Groups</h4>

    <ul>
        <li><a href="{{ urlFor('api.groups') }}">{{ urlFor('api.groups') }}</a></li>
        <li><a href="{{ urlFor('api.group', {id: ':id'}) }}">{{ urlFor('api.group', {id: ':id'}) }}</a></li>
    </ul>

    <h4>Users</h4>

    <ul>
        <li><a href="{{ urlFor('api.users') }}">{{ urlFor('api.users') }}</a></li>
        <li><a href="{{ urlFor('api.user', {id: ':id'}) }}">{{ urlFor('api.user', {id: ':id'}) }}</a></li>
    </ul>

    <h4>Repositories</h4>

    <ul>
        <li><a href="{{ urlFor('api.repositories') }}">{{ urlFor('api.repositories') }}</a></li>
        <li><a href="{{ urlFor('api.repository', {id: ':id'}) }}">{{ urlFor('api.repository', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.repository.tags', {id: ':id'}) }}">{{ urlFor('api.repository.tags', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.repository.branches', {id: ':id'}) }}">{{ urlFor('api.repository.branches', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.repository.pullrequests', {id: ':id'}) }}">{{ urlFor('api.repository.pullrequests', {id: ':id'}) }}</a></li>
    </ul>

    <h4>Deployments</h4>

    <ul>
        <li><a href="{{ urlFor('api.deployments', {id: ':id'}) }}">{{ urlFor('api.deployments', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.deployment', {id: ':id'}) }}">{{ urlFor('api.deployment', {id: ':id'}) }}</a></li>
    </ul>

    <h4>Builds</h4>

    <ul>
        <li><a href="{{ urlFor('api.builds', {id: ':id'}) }}">{{ urlFor('api.builds', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.build', {id: ':id'}) }}">{{ urlFor('api.build', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.build.logs', {id: ':id'}) }}">{{ urlFor('api.build.logs', {id: ':id'}) }}</a> [NYI]</li>
    </ul>

    <h4>Pushes</h4>

    <ul>
        <li><a href="{{ urlFor('api.pushes', {id: ':id'}) }}">{{ urlFor('api.pushes', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.push', {id: 'id'}) }}">{{ urlFor('api.push', {id: ':id'}) }}</a></li>
        <li><a href="{{ urlFor('api.push.logs', {id: ':id'}) }}">{{ urlFor('api.push.logs', {id: ':id'}) }}</a> [NYI]</li>
    </ul>

    <h3>Protected Endpoints</h3>

    <p>Protected endpoints require client authentication, but allow you to interact with the build and push system.</p>

{% endblock %}
